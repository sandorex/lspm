## lspm
LSP Manager is utility to easily start LSPs inside a container from any LSP client (emacs, neovim, kate, helix)

### How does it work
`lspm` does few things but most notably translates paths from the host to container paths otherwise that confuses the editor

### Goal
Goal is to have small containers with specific LSPs you can quickly develop any language without setting up tooling
